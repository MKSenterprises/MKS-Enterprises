import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```

**FILE 5** — Save as `App.jsx`
Go to the grocery portal artifact → click the **copy button** (top right of artifact) → paste into Notepad → save as `App.jsx`

---

### ⚠️ Important — folder structure
Your Desktop folder must look like this:
```
mks-grocery/
├── index.html
├── package.json
├── vite.config.js
└── src/
    ├── main.jsx
    └── App.jsx